<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kotlearn.kotlearn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityLesson" />
        <activity android:name=".ActivityTypes" />
        <activity android:name=".ActivityString" />
        <activity android:name=".ActivityArray" />
        <activity android:name=".ActivityLoop" />
        <activity android:name=".ActivityFun" />
        <activity android:name=".ActivityChoosePokemon"></activity>
    </application>

</manifest>